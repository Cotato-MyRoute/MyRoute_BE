package BE.MyRoute.route.repository;

import BE.MyRoute.route.entity.RHashtag;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RHashtagRepository extends JpaRepository<RHashtag, Long> {
}
